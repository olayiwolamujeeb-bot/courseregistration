{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "php artisan serve",
        "build": "echo \"No frontend build step is required.\""
    }
}
